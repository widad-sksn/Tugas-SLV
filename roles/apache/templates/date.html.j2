<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Time Sync</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;600;800&display=swap" rel="stylesheet">
    <style>
        body { background-color: #0f172a; color: #e2e8f0; font-family: 'Inter', sans-serif; display: flex; flex-direction: column; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        h1 { font-size: 8rem; margin: 0; font-weight: 700; letter-spacing: -4px; color: #60a5fa; text-shadow: 0 10px 30px rgba(96, 165, 250, 0.3); }
        .date { color: #94a3b8; margin-top: 10px; font-size: 2rem; font-weight: 300; }
        .server-tag { background: #1e293b; padding: 10px 30px; border-radius: 50px; font-size: 1.2rem; margin-top: 40px; color: #64748b; border: 1px solid #334155; }
        .back-link { margin-top: 50px; color: #64748b; text-decoration: none; border-bottom: 2px dashed #64748b; font-size: 1.2rem; padding-bottom: 5px; transition: 0.3s; }
        .back-link:hover { color: #e2e8f0; border-color: #e2e8f0; }
    </style>
</head>
<body>
    <h1 id="clock">{{ ansible_date_time.time }}</h1>
    <div class="date">{{ ansible_date_time.date }}</div>
    <div class="server-tag">Synced with Node: {{ inventory_hostname }}</div>
    <a href="/" class="back-link">Kembali ke Dashboard Utama</a>
    <script>setInterval(() => { document.getElementById('clock').innerText = new Date().toLocaleTimeString('id-ID'); }, 1000);</script>
</body>
</html>
